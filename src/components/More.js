import React from "react";
import { sidebarItemsData } from "../data/SidebarData";
import styled from "styled-components";

const Container = styled.div``;

const More = () => {
  return <Container>{sidebarItemsData.map((items) => {})}</Container>;
};

export default More;
